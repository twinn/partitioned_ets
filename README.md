# EtsPartitioned

**TODO: Add description**
**TODO: Support registering functions for when nodes are added and removed**
**TODO: Get feedback on use macro vs module interaction**
**TODO: Support for additional options [keypos, private, protected]**

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed
by adding `ets_partitioned` to your list of dependencies in `mix.exs`:

```elixir
def deps do
  [
    {:ets_partitioned, "~> 0.1.0"}
  ]
end
```

Documentation can be generated with [ExDoc](https://github.com/elixir-lang/ex_doc)
and published on [HexDocs](https://hexdocs.pm). Once published, the docs can
be found at <https://hexdocs.pm/ets_partitioned>.

